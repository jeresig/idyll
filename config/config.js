module.exports = {
    development: {
        server: {
            port: 3000,
        },

        db: "mongodb://localhost/selection"
    },

    production: {
        server: {
            port: 3000,
        },

        db: "mongodb://localhost/selection"
    }
};
